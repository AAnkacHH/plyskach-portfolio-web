frontend:
    path: .
    publish: dist
    scripts:
        build: npm run build
        deploy: npm install
        start:
            - npm install --silent
            - npm run dev --silent
    subdomain: plyskach-portfolio-web
name: plyskach-portfolio-web
region: eu-central-1
yamlVersion: 2
